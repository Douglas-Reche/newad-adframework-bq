# Arquivos legados — schema Gold pré-rebuild

Movidos para esta pasta em 2026-06-24, ao iniciar a reconstrução da Gold layer pós-rebuild RAW+STG. Todos referenciam tabelas RAW/STG já dropadas (`stg.mgid_campaigns`, `stg.siprocal_delivery`, `core.campaign_format_map`, etc.) ou o schema antigo de `gold.fact_io_plan`/`fact_pacing`.

Preservados como referência histórica de decisões de negócio (ex: lógica de `category` por plataforma) — não executar.

Ver `docs/gold_layer_design.md` para o design vigente.
